<!DOCTYPE html>
<html lang="en">
<head>
  <title>Photo</title>
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,300,600' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
	
	<div class="navbar navbar-fixed-top">
	    <div class="navbar-inner">
	      <div class="container">
	        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
	          <span class="icon-bar"></span>
	          <span class="icon-bar"></span>
	          <span class="icon-bar"></span>
	        </a>
	        <a class="brand" href=<%= paintings_path %>>SomePhotos</a>
	        <div class="nav-collapse">


	    <ul class="nav pull-right">
	          	<li><%= link_to "Buy Photos", '/paintings/index_admin' %></li>
	            <li><%= link_to "Sell Photos", new_painting_path %></li>
	            <li> <%= form_tag paintings_path, :method => 'get', :id => "paintings_search" do %>
		<%= text_field_tag :search, params[:search] %>
			<%#= submit_tag "Search",class: 'btn btn-info', :name => nil %>
<!--		<form class="navbar-search pull-right"><input type="text" class="search-query" placeholder="Find Photos"> </form>
-->
		</li>
<% end %>

	               <ul class="nav pull-right">
                    <li class="dropdown">
                      <a href="#" id="accountdrop" role="button" class="dropdown-toggle" data-toggle="dropdown"> Me<b class="caret"></b></a>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="drop3">
                        <li><%= link_to "My Paintings", '/paintings/mypaintings', tabindex:'-1'%></li>
                        <li><a tabindex="-1" href="#">Photos I've bought</a></li>
                        <li><a tabindex="-1" href="#">Photos I've sold</a></li>
                        <li><a tabindex="-1" href="#">Settings</a></li>
                        <li class="divider"></li>
                        <li><%= link_to "Sign out", logout_path, tabindex: '-1' %></li>
                      </ul>
                    </li>
                   </ul>
        </ul>


	        </div>
	      </div>
	    </div>
	  </div>
	
	<div class="container">
	
	<% flash.each do |k, v| %>
	      <div class="<%= k %>"><%= v %></div>
	  <% end %>
	

	

<!--
	<div class="alert alert-info">	
	<% if current_user %>
	  Logged in as <%= current_user.email %> and <%= current_user.id %>
	  <%= link_to "Log Out", logout_path %>
	<% else %>
	  <%= link_to "Sign Up", signup_path %> or
	  <%= link_to "Log In", login_path %>
	<% end %>
	</div>
-->
<%= yield %>

</div>
</body>
</html>


